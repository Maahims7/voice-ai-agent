# voice-ai-agent
Voice-Controlled Local AI Agent using Whisper, Ollama (LLaMA3), and Streamlit. Converts speech to text, detects user intent, executes local actions, and displays the full pipeline in a clean UI.
